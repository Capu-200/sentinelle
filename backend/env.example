# Exemple de configuration pour le développement local
# Copiez ce fichier en .env et remplissez les valeurs

# ========== ML ENGINE ==========
# URL du ML Engine (Cloud Run Service)
# Récupérer avec : gcloud run services describe sentinelle-ml-engine --region=europe-west1 --project=sentinelle-485209 --format="value(status.url)"
ML_ENGINE_URL=https://sentinelle-ml-engine-ntqku76mya-ew.a.run.app

# ========== DATABASE ==========
# Pour développement local avec Docker
# DATABASE_URL=postgresql+psycopg2://fraud_user:fraud_pwd@localhost:5432/fraud_db

# Pour développement local avec Cloud SQL (via Cloud SQL Auth Proxy)
# DATABASE_URL=postgresql+psycopg2://fraud_user:VOTRE_MOT_DE_PASSE@127.0.0.1:5432/fraud_db

# Pour Cloud Run (configuré automatiquement)
# DATABASE_URL=postgresql+psycopg2://fraud_user:${DB_PASSWORD}@/${DB_NAME}?host=/cloudsql/CONNECTION_NAME

